---
title: Inicio rápido
description: Instalá el paquete, configurá credenciales y creá tu primera preference de Mercado Pago en un proyecto Laravel.
icon: rocket
---

## Requisitos previos

- Laravel `^12.0`
- PHP `^8.2`
- credenciales de Mercado Pago
- una app local corriendo en `http://localhost:8000`

## 1. Instalá el paquete

```bash
composer require fitodac/laravel-mercadopago:^1.0
```

## 2. Configurá `.env`

```ini
APP_ENV=local
MERCADOPAGO_ACCESS_TOKEN=TEST-your-access-token
MERCADOPAGO_PUBLIC_KEY=TEST-your-public-key
MERCADOPAGO_WEBHOOK_SECRET=test-secret
MERCADOPAGO_ENABLE_DEMO_ROUTES=true
```

## 3. Verificá las rutas

```bash
php artisan route:list --name=mercadopago
```

## 4. Revisá el estado del paquete

```bash
curl http://localhost:8000/api/mercadopago/health
```

## 5. Creá una preference

```bash
curl --request POST \
  --url http://localhost:8000/api/mercadopago/preferences \
  --header 'Content-Type: application/json' \
  --data '{
    "items": [
      {
        "title": "Starter product",
        "quantity": 1,
        "unit_price": 100.50
      }
    ],
    "payer": {
      "email": "buyer@example.com"
    },
    "back_urls": {
      "success": "https://your-app.test/payments/success",
      "pending": "https://your-app.test/payments/pending",
      "failure": "https://your-app.test/payments/failure"
    },
    "notification_url": "https://your-app.test/api/mercadopago/webhooks",
    "external_reference": "order-1001"
  }'
```

Forma esperada de la respuesta:

```json
{
  "ok": true,
  "data": {
    "id": "1234567890",
    "init_point": "https://www.mercadopago.com/checkout/v1/redirect?pref_id=...",
    "sandbox_init_point": "https://sandbox.mercadopago.com/checkout/v1/redirect?pref_id=..."
  },
  "meta": []
}
```

## 6. Pasá de demo a código de producción

Cuando el paquete ya esté cableado, inyectá `PreferenceService` en tu propio controller y dejá de depender del endpoint demo para flujos visibles al usuario.

## Siguientes pasos

- [Crear preferences](/es/guides/creating-preferences)
- [Manejar webhooks](/es/guides/handling-webhooks)
- [Testing](/es/guides/testing)
