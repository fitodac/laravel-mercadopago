---
title: Instalación
description: Instalá Laravel MercadoPago, publicá la configuración si hace falta y verificá las rutas del paquete.
---

## Requisitos

Antes de instalar el paquete, confirmá que tu proyecto usa:

- PHP `^8.2`
- Laravel `^12.0`
- Composer

## Instalación estándar

Instalá el paquete en cualquier proyecto Laravel:

```bash
composer require fitodac/laravel-mercadopago:^1.0
```

Laravel auto-descubre:

```php
Fitodac\LaravelMercadoPago\LaravelMercadoPagoServiceProvider::class
```

## Flujo con `path repository`

Si el paquete vive dentro del mismo workspace, agregá un `path` repository al `composer.json` de la app host:

```json
{
  "repositories": [
    {
      "type": "path",
      "url": "plugins/laravel-mercadopago",
      "options": {
        "symlink": true
      }
    }
  ]
}
```

Después instalalo con el mismo comando:

```bash
composer require fitodac/laravel-mercadopago:^1.0
```

## Opcional: publicar configuración

Solo necesitás esto si querés tener `config/mercadopago.php` dentro de tu proyecto:

```bash
php artisan vendor:publish --tag=mercadopago-config
```

## Verificar el registro del paquete

Ejecutá:

```bash
php artisan package:discover
php artisan route:list --name=mercadopago
```

Deberías ver nombres de rutas como:

- `mercadopago.webhooks.store`
- `mercadopago.health`
- `mercadopago.preferences.store`
- `mercadopago.payments.store`

<Note>
  Las demo routes solo responden cuando `MERCADOPAGO_ENABLE_DEMO_ROUTES=true` y
  el entorno Laravel es `local` o `testing`. La ruta de webhook siempre queda
  registrada.
</Note>

## Configuración mínima en `.env`

Definí al menos tu access token:

```ini
MERCADOPAGO_ACCESS_TOKEN=APP_USR-your-access-token
MERCADOPAGO_PUBLIC_KEY=APP_USR-your-public-key
MERCADOPAGO_WEBHOOK_SECRET=your-webhook-secret
MERCADOPAGO_ROUTE_PREFIX=api/mercadopago
MERCADOPAGO_ENABLE_DEMO_ROUTES=true
MERCADOPAGO_RUNTIME_ENVIRONMENT=
```

<Warning>
  Guardá las credenciales reales en `.env` o en un secret manager. Nunca las
  subas al repositorio.
</Warning>

## Primera verificación

Con demo routes habilitadas en `local` o `testing`, llamá a:

```bash
curl http://localhost:8000/api/mercadopago/health
```

Forma esperada:

```json
{
  "ok": true,
  "data": {
    "configured": true,
    "has_public_key": true,
    "has_webhook_secret": true,
    "environment": "local"
  },
  "meta": []
}
```

## Siguientes pasos

- Seguí con [Configuración](/es/configuration)
- Ejecutá la [Guía rápida](/es/guides/quickstart)
- Revisá la [Arquitectura](/es/concepts/architecture)
