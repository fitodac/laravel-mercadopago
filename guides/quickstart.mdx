---
title: Quickstart
description: Install the package, configure credentials, and create your first Mercado Pago preference in a Laravel project.
icon: rocket
---

## Prerequisites

- Laravel `^12.0`
- PHP `^8.2`
- Mercado Pago credentials
- a local app running on `http://localhost:8000`

## 1. Install the package

```bash
composer require fitodac/laravel-mercadopago:^1.0
```

## 2. Configure `.env`

```ini
APP_ENV=local
MERCADOPAGO_ACCESS_TOKEN=TEST-your-access-token
MERCADOPAGO_PUBLIC_KEY=TEST-your-public-key
MERCADOPAGO_WEBHOOK_SECRET=test-secret
MERCADOPAGO_ENABLE_DEMO_ROUTES=true
```

## 3. Verify routes

```bash
php artisan route:list --name=mercadopago
```

## 4. Check package health

```bash
curl http://localhost:8000/api/mercadopago/health
```

## 5. Create a preference

```bash
curl --request POST \
  --url http://localhost:8000/api/mercadopago/preferences \
  --header 'Content-Type: application/json' \
  --data '{
    "items": [
      {
        "title": "Starter product",
        "quantity": 1,
        "unit_price": 100.50
      }
    ],
    "payer": {
      "email": "buyer@example.com"
    },
    "back_urls": {
      "success": "https://your-app.test/payments/success",
      "pending": "https://your-app.test/payments/pending",
      "failure": "https://your-app.test/payments/failure"
    },
    "notification_url": "https://your-app.test/api/mercadopago/webhooks",
    "external_reference": "order-1001"
  }'
```

Expected response shape:

```json
{
  "ok": true,
  "data": {
    "id": "1234567890",
    "init_point": "https://www.mercadopago.com/checkout/v1/redirect?pref_id=...",
    "sandbox_init_point": "https://sandbox.mercadopago.com/checkout/v1/redirect?pref_id=..."
  },
  "meta": []
}
```

## 6. Move from demo to production code

Once the package is wired correctly, inject `PreferenceService` into your own controller and stop depending on the demo endpoint for user-facing flows.

## Next steps

- [Creating preferences](/guides/creating-preferences)
- [Handling webhooks](/guides/handling-webhooks)
- [Testing](/guides/testing)
